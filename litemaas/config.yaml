# LiteMaas (LiteLLM) proxy configuration
# All AI calls from the bot go through this proxy.

#model_list:
#  - model_name: gpt-4o-mini
#    litellm_params:
#      model: openai/gpt-4o-mini
#      api_key: os.environ/OPENAI_API_KEY

#  - model_name: gpt-4o
#    litellm_params:
#      model: openai/gpt-4o
#      api_key: os.environ/OPENAI_API_KEY

litellm_settings:
  # Drop unsupported params instead of raising errors
  drop_params: true
  # Don't expose real API keys in responses
  redact_user_api_key_info: true

general_settings:
  master_key: os.environ/LITELLM_MASTER_KEY
  # LiteLLM will use this DB for its own internal tracking (SpendLogs, etc.)
  database_url: os.environ/DATABASE_URL
  store_model_in_db: true
